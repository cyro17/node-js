// const http = require("http");
// const routes = require("./routes");

// const server = http.createServer(routes.handler);
// server.listen(8000);

const person = {
  name: "ronit",
  age: 23,
  greet() {
    console.log("hey there is am " + `${name}`);
  },
};
